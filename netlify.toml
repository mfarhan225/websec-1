[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_ENV = "production"
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Aktifkan Next.js Runtime di Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# --- SECURITY HEADERS (CDN layer) ---

# Header umum untuk semua path
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
    X-Frame-Options = "DENY"
    X-DNS-Prefetch-Control = "off"
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Resource-Policy = "same-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Strict-Transport-Security = "max-age=63072000; includeSubDomains; preload"
    Content-Security-Policy = "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; media-src 'self' blob:; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests"

# Jangan cache halaman sensitif
[[headers]]
  for = "/dashboard/*"
  [headers.values]
    Cache-Control = "no-store"

# Jangan cache respons API
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
